.show-edit {
  @extends $form;

  footer {
    @extends $clearfix;
  }

  a {
    color: darken(error-color, 20%);
    float: left;
    margin-top: 0.6em;

    +hover() {
      color: error-color;
    }
  }
}
