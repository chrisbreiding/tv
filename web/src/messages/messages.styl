.messages {
  left: 0;
  position: fixed;
  right: 0;
  text-align: center;
  top: 0.5em;

  li {
    background: darkest-color;
    border: solid 1px lighten(dark-color, 10%);
    border-radius: 3px;
    display: inline-block;
    margin: 0 0.5em;
    padding: 0.5em 1em;
  }
}
